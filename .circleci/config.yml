on: 2.1
orbs:
  docker: circleci/docker@1.0.0
  heroku: circleci/heroku@1.0.1
workflows:
  docker_deploy:
    jobs:
      - docker/publish:
          image: registry.heroku.com/pipelineexercise/web
          tag: 'latest'
  heroku_deploy:
    jobs:
      - heroku/deploy-via-git
